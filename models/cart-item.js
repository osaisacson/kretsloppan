class CartItem {
  constructor(image, quantity, price, productTitle) {
    this.image = image;
    this.quantity = quantity;
    this.price = price;
    this.productTitle = productTitle;
  }
}

export default CartItem;
